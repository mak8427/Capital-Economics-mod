# Maintainer Note:
# - Purpose: mvthem_script_values.txt defines scripted value formulas used by the Pantalone_Economics feature set.
# - Reasoning: keeping this logic in a dedicated file isolates balancing and avoids accidental vanilla overlap.
# - Maintenance: when renaming keys/scopes, update cross-file references and localization entries in the same change.

# Script values for Move Them cabinet action
mvthem_move_population_monthly_transfer = {
	if = {
		limit = {
			scope:target_1 = { has_variable = mvthem_move_pop_active }
		}
		value = scope:target_1.population
		multiply = 5
		if = {
			limit = { exists = scope:cabinet }
			add = {
				value = scope:target_1.population
				multiply = 5
				multiply = {
					value = "scope:cabinet.effective_skill" 
					 
				}
			}
		} 
		max = 1000
		min = 100
	}
	else = {
		value = 0
	}
}

mvthem_move_population_monthly_transfer_display = {
	value = mvthem_move_population_monthly_transfer
}
